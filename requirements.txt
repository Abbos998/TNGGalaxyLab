# TNGGalaxyLab dependencies (tested with Python 3.12.7)
#
# Install with:
#     pip install -r requirements.txt
#
# For an isolated environment (recommended):
#     python -m venv venv
#     source venv/bin/activate   # or venv\Scripts\activate on Windows
#     pip install -r requirements.txt
#
# Version ranges are chosen for compatibility with the tested environment
# while allowing minor upgrades within stable API windows.

numpy>=1.24,<2.0
scipy>=1.11,<2.0
matplotlib>=3.7,<4.0
pandas>=2.0,<3.0
h5py>=3.9,<4.0
astropy>=5.3,<7.0
pyyaml>=6.0
requests>=2.28

# Development / test dependencies (optional; used only by CI)
# Uncomment for full validation workflow:
# pytest>=7.0
# pytest-cov>=4.0
